(setq mew-name "taozhijiang") ;; (user-full-name)
(setq mew-user "taozhijiang@tp-link.com.cn") ;; (user-login-name)
(setq mew-mail-domain "mail.tp-link.com.cn")

(setq mew-smtp-server "smtp.tp-link.com.cn")
;;(setq mew-smtp-server "your SMTP server")  ;; if not localhost

(setq mew-pop-user "taozhijiang")  ;; (user-login-name)
(setq mew-pop-server "pop3.tp-link.com.cn")    ;; if not localhost

(setq mew-pop-auth 'pass)
(setq mew-use-cached-passwd t)
(setq mew-use-master-passwd t)


